name: wf-ci
on:
  pull_request:
    branches:
      - develop
    types:
      - opened
      - synchronize
    
  push:
    branches:
      - develop
jobs:
  linting-job:
    runs-on: ubuntu-latest
    steps:
      - name: Wellcome steps
        run: echo "Wellcome to CI pipeline"
      - name: Checkout Repo
        uses: actions/checkout@v4